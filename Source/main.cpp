#include <Engine.h>

int main() {
    Engine e;

    /*Table* table = new Table();

    table->moveInAdvance("(e2, e4), (b8, a6), (d2, d4), (b7, b6), (d4, d5), (a6, b4), (b1, c3), (a7, a5), (f1, c4), (b4, a6), (c4, a6), (c8, a6), (d1, f3), (a8, c8), (c1, e3), (d7, d6), (e1, c1), (g8, f6), (g1, h3), (g7, g6), (h3, f4), (h7, h6), (h1, e1), (e7, e6), (d5, e6), (f8, g7), (e6, f7), (e8, f7), (c3, d5)", 'w');
    table->printGameBoard();
    std::cout << table->getBestMove(4);*/

    //Table* table = new Table();

    //std::cout << table->degreesOfFreedoom();

    return 0;
}

