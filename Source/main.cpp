#include <Engine.h>

int main() {
    //Engine e;

    /*Table* table = new Table();

    table->moveInAdvance("(e2, e4), (b7, b6), (d2, d4), (d7, d6), (c2, c4), (c8, h3), (g1, h3), (f7, f6), (e4, e5), (f6, f5), (e5, e6), (d8, c8), (d4, d5), (b8, d7), (f1, d3), (a7, a6), (d3, f5), (d7, b8), (f5, g6), (e8, d8), (g6, f7)", 'w');
    table->printGameBoard('w', true, true);

    table->markAllPossibleMoves();

    for (int i = 0; i < table->height; i++)
        for (int j = 0; j < table->width; j++) {
            std::cout << i << ", " << j << ": ";

            for (ChessPiece* chessPiece : table->squares[i][j]->possibleNormalMoves)
                std::cout << chessPiece->pos.toString() << " ";

            std::cout << "\n";
        }*/

    return 0;
}
