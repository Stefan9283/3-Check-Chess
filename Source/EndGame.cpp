#include "EndGame.h"
#include "Table.h"

float PiecesVsKing::MiniMax(TreeNode* root, int depth, float alpha, float beta, int priority, bool maximizingPlayer) {
	return 0.0f;
}

std::string PiecesVsKing::getBestMove(Table* table, int depth) {
	if (advantageousPart == table->turn) {

	} else {

	}

	return std::string("move");
}
